import React, { Component } from "react";

export default class Add extends Component {
  render() {
    return (
      <div>
        <h1>Should contain a form to add new user</h1>
      </div>
    );
  }
}
