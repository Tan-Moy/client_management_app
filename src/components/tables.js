import React, { Component } from "react";

export default class Tables extends Component {
    render() {
        return (
            <div>
                These are tables.
            </div>
        );
    }
}
