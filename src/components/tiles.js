import React, { Component } from "react";

export default class Tiles extends Component {
    render() {
        return (
            <div>
                These are tiles
            </div>
        );
    }
}
